il debugger sta solo in cambiaPelle() @ utilogic






Come funziona:
la prima pagina che l'utente riceve e' pw.html da dove
vengono importate le varie librerie.E' anche il 'main'.

Tira giu la home.xml e la gira subito al DF di default,
alla fine di questa comunicazione la home viene
visualizzata da homeManeggio().

pw.html -> tiragiu(home.xml) -> homeManeggio() -> home.html


-i vari rispondi() scrivi() //edo
-onSuccess di query
-problema con la ricezione del link al pdf con Tigella, la formattazione comunque va a buon fine
# Nella mia intenzione questo file servirebbe per tenere traccia di che cosa va fatto e 
# di chi se ne sta occupando.
 
1) replyTo(scheda, reply){} -> edo
	verificare comportamento con browser diversi da FF3.
	gestire correttamente(?) le folksonomie
